apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: external-asm-gateway-test
spec:
  selector:
    istio: ingressgateway-ext
  servers:
  - port:
      number: 443
      name: https
      protocol: HTTPS
    tls:
      mode: SIMPLE
      credentialName: external-gw-tls-credential
    hosts:
    - feitopssreaks001.ferguson.com
